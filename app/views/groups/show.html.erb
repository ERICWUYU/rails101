<div class="col-md-12">
    <div class="group d-flex justify-content-end">
        <% if current_user && current_user == @group.user%>
            <%= link_to("Edit", edit_group_path(@group), class: "btn btn-primary m-2") %>
        <% end %>
    </div>
    <h2><%= @group.title %></h2>
    <p><%= @group.description %></p>
</div>